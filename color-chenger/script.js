const boxs = document.querySelectorAll(".button");
const body = document.querySelector("body");
boxs.forEach(function (iteam) {

    iteam.addEventListener("click", function (e) {
        let id = e.target.id;
        if (id == id) {
            body.style.backgroundColor = id;
        } if (id == id) {
            body.style.backgroundColor = id;
        } if (id == id) {
            body.style.backgroundColor = id;
        } if (id == id) {
            body.style.backgroundColor = id;
        }
    })

})